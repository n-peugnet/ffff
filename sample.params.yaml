site:
  name: Nicolas Peugnet
  description:

defaults:
  sort:
    type: alpha
    order: asc
  render: title
  layout: default
  date formats:
    - d/m/Y H:i:s
    - d/m/Y H:i
    - d/m/Y

# you don't really need to edit these settings
system:
  dirs:
    public: public
    temp: tmp